apiVersion: unleash.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: unleash-creds
      namespace: default
      key: credentials
